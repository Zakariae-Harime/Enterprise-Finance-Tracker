# Azure ML training environment — GPU container dependencies
# These are installed on TOP of the PyTorch 2.1 + CUDA 11.8 base image.
# Keep this minimal — every extra package adds Docker build time.

# NLP — BERT fine-tuning
transformers>=4.36.0      # HuggingFace: loads NbAiLab/nb-bert-base + Trainer API
datasets>=2.16.0          # HuggingFace Datasets: efficient Parquet loading for training

# ML — Layer 2 (TF-IDF + LogisticRegression, trained alongside BERT)
scikit-learn>=1.4.0
joblib>=1.3.0

# ONNX export (runs at end of training job to produce the inference artifact)
onnx>=1.15.0
onnxruntime>=1.17.0

# Data
pandas>=2.1.0
pyarrow>=15.0.0
numpy>=1.24.0
