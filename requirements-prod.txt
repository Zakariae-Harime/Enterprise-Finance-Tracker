# === Web Framework ===
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# === Database ===
asyncpg>=0.29.0

# === Kafka ===
aiokafka>=0.10.0

# === ML — Inference only (no PyTorch/transformers — we run ONNX) ===
scikit-learn>=1.4.0       # TF-IDF vectorizer + LogisticRegression
joblib>=1.3.0             # Load .joblib model files
onnxruntime>=1.17.0       # Runs categorizer_int8.onnx — no PyTorch needed

# === Data Processing ===
pandas>=2.1.0
pyarrow>=15.0.0

# === ERP Adapters ===
aiohttp>=3.9.0            # Async HTTP client — used by all 6 ERP adapters

# === Utilities ===
python-jose[cryptography]
passlib[bcrypt]
cryptography>=42.0.0      # Fernet symmetric encryption for ERP credentials
httpx>=0.26.0
uuid6>=2024.1.12

# === Azure Data Lake ===
azure-storage-file-datalake>=12.14.0
azure-storage-blob>=12.19.0
azure-identity>=1.15.0
