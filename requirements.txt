# === Web Framework ===
fastapi>=0.109.0          # Async web framework (auto-generates OpenAPI docs)
uvicorn[standard]>=0.27.0 # ASGI server to run FastAPI
pydantic>=2.5.0           # Data validation using Python type hints

# === Database ===
asyncpg>=0.29.0           # Async PostgreSQL driver (10x faster than psycopg2)
sqlalchemy>=2.0.0         # ORM

# === Kafka ===
aiokafka>=0.10.0          # Async Kafka client

# === ML — Core ===
torch>=2.1.0              # PyTorch: the deep learning framework BERT runs on
transformers>=4.36.0      # HuggingFace: loads NbAiLab/nb-bert-base + Trainer API
scikit-learn>=1.4.0       # TF-IDF vectorizer + LogisticRegression (Layer 2)
joblib>=1.3.0             # Save/load trained sklearn models to disk (.joblib files)
datasets>=2.16.0          # HuggingFace Datasets: efficient data loading during training

# === ML — Inference (ONNX) ===
onnx>=1.15.0              # ONNX model format: portable, framework-independent
onnxruntime>=1.17.0       # ONNX Runtime: 10x faster inference than raw PyTorch

# === ML — Azure Training ===
azure-ai-ml>=1.12.0       # Azure ML v2 SDK: submit training jobs from your laptop

# === Data Processing ===
pandas>=2.1.0             # DataFrames for aggregations

# === Utilities ===
python-jose[cryptography] # JWT tokens for auth
passlib[bcrypt]           # Password hashing
httpx>=0.26.0             # Async HTTP client (for GoCardless API)
uuid6>=2024.1.12          # UUID7 support (time-ordered UUIDs, RFC 9562)
# === Azure Data Lake ===
azure-storage-file-datalake>=12.14.0  # Data Lake Gen2 SDK
azure-storage-blob>=12.19.0      # Azure Blob Storage SDK
azure-identity>=1.15.0           # Azure authentication
pyarrow>=15.0.0                  # Parquet file format (columnar, compressed)

# === Azure OCR ===
azure-ai-formrecognizer>=3.3.0

# === Airflow Azure Provider ===
apache-airflow-providers-microsoft-azure>=8.5.0